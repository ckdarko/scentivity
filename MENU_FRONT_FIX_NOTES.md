# Menu Front-Layer Fix

Changes made:
- Raised the menu popup above the homepage video.
- Added high z-index and isolation to the header menu panel/card.
- Lowered the homepage video layer while the menu is open.
- Added CSS cache busting.

Changed files only:
- index.html
- styles.css

Deploy:
1. Upload only `index.html` and `styles.css` to GitHub repository root.
2. Netlify → Deploys → Trigger deploy → Clear cache and deploy site.
