# Scentivity PerfumeGH-Style Menu Update

Changes made:
- The top-right menu now opens like the sample image: white panel, close button, search bar, large vertical menu items.
- Menu includes: SHOP, PREORDER, PRODUCT CATALOGUE, ABOUT US, CART.
- Search bar can search store products when the user types and presses Enter.
- This update only requires index.html and styles.css.

Changed files only:
- index.html
- styles.css

Deploy:
1. Upload only `index.html` and `styles.css` to GitHub repository root if the previous update is already uploaded.
2. Netlify → Deploys → Trigger deploy → Clear cache and deploy site.
